// Needs fed a list of clients

<h2>Clients:</h2>

@if (Model.Count != 0)
{
  <ul>
    @foreach (var client in Model)
    {
      <li><a href='/clients/@client.Id'>@client.Name</a></li>
    }
  </ul>
}

<h4><a href="/">Return home</a></h4>
