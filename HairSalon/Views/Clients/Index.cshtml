@* @using HairSalon.Models;

<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>Welcome to the Three Stooges Hair Salon!</title>
  <link rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css'>
  <link rel="stylesheet" href="~/css/styles.css">
</head>
<body class="container">
  <h1>Here are the restaurants!</h1>

  <ul>
    @if (@Model.Count == 0)
    {
      <p>There are no restaurants :(!</p>
    }
      @if (@Model.Count != 0)
      {
        @foreach (Restaurant restaurant in Model)
        {
          <li><a href='/restaurants/@restaurant.Id'>@restaurant.Name</a></li>
        }
      }
      </ul>

      <a href="/restaurants/new">Add a restaurant!</a>

      <form action="/restaurants/delete" method="post">
      <button type="submit" name="button">Clear All Restaurants</button>
      </form>

      </html>
      </body> *@
